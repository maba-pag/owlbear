# skills/

Transition placeholder directory. The authoritative skill definitions live in
`.github/skills/`. This directory will become the primary location during the
v2 migration. VS Code settings point to both locations.
