// RED-phase stub — minimal shell so tests compile.
// Builder replaces this with the real implementation.

export interface ActivityTabProps {
  onSelectTask?: (taskId: number, subtab?: string) => void
}

export default function ActivityTab(_props: ActivityTabProps): null {
  return null
}
