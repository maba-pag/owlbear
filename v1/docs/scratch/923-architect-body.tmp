Research follow-up from docs/research/bearclaw-board-command-red-gate.md and docs/research/bearclaw-board-kanban-json-fixtures.md.

Optional RED cleanup after #909 and #910 land.

AC:
1. In tests/test_cli_board_fixtures.py, add failing contract tests for an importable board-only helper module tests/cli_board_fixtures.py exposing board_task(...), board_move(...), and board_payloads(...) or equivalent helper names that emit paired kanban-md list --json and kanban-md log --action move --json payload strings from shared semantic inputs.
2. Cover assignee precedence and age-in-status fixture cases, including created fallback and ignoring move entries whose destination does not match the task's current status.
3. In tests/test_cli_board.py, update at least two existing happy-path board-command cases to use the helper while keeping subprocess command routing local to the file and leaving current CLI-boundary assertions unchanged.
4. Scope stays under tests/ only, does not modify tests/conftest.py or src/, adds no new dependency, and does not absorb #926's generic subprocess-result-helper lane.
5. All newly added assertions fail before the follow-on helper-implementation task lands.

## Research

Doc: docs/research/bearclaw-board-kanban-json-fixtures.md

Summary:

- Board JSON fixture cleanup should stay board-specific; do not widen tests/conftest.py.
- Recommended shape: tests/cli_board_fixtures.py with board_task(), board_move(), and board_payloads(); keep subprocess routing local to tests/test_cli_board.py.
- Scope boundary: #923 should not absorb #926's generic CompletedProcess helper lane.

Follow-up tasks created:

- #936 Test board-specific kanban JSON payload helper for BearClaw board CLI tests (RED)
- #939 Implement board-specific kanban JSON payload helper for BearClaw board CLI tests

Commands executed:

- kanban\kanban-md.exe create --title "Test board-specific kanban JSON payload helper for BearClaw board CLI tests (RED)" ...
- kanban\kanban-md.exe create --title "Implement board-specific kanban JSON payload helper for BearClaw board CLI tests" ...

Attribution:

- Added pytest fixtures reference, pytest how-to fixtures, Python json docs, and Typer Testing docs to docs/sources/overview.md.