# instructions/

Transition placeholder directory. The authoritative instruction files live in
`.github/instructions/`. This directory will become the primary location during
the v2 migration. VS Code settings point to both locations.
