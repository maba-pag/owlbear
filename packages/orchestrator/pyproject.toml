[project]
name = "owlbear"
version = "0.1.0"
requires-python = ">=3.12"
description = "OwlBear orchestrator — core package"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/owlbear", "src/owlbear_orchestrator"]
