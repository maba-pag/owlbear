[project]
name = "owlbear-mcp-kanban"
version = "0.1.0"
requires-python = ">=3.12"
description = "OwlBear MCP server — kanban operations"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/owlbear_mcp_kanban"]
