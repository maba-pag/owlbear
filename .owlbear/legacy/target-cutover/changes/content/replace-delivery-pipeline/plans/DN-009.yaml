---
mode: verification-only
packets: []
candidate_revision: 7805c43199f1797c61912c4ef54083802724c8ef
generation:
  job_id: 70
  receipt_id: admission-3212969f2164
  predecessor_job_ids: []
obligations:
  - REQ-020
  - REQ-009
  - REQ-015
  - REQ-016
  - REQ-017
  - NEG-001
  - KEEP-006
  - KEEP-007
modules:
  - MOD-002
  - MOD-008
interfaces:
  produces:
    - IF-010
  consumes:
    - IF-001
    - IF-002
    - IF-003
    - IF-004
    - IF-015
dependency_contracts:
  - DN-004
risks:
  - RISK-005
proof: PROOF-011
source_inspection:
  disposition: pass
  evidence: At the exact candidate, server.py exports and registers the strict
    native control plane over the real graph-aware engine; models.py supplies
    strict parameter schemas; the maintained surface, request, and native query
    suites cover registry equality, public lifecycle and admission behavior,
    request identity and replay/conflict semantics, paged job, attempt,
    activity, and receipt projections, health diagnostics, and absence of
    generic task mutation and retired lifecycle operations. The produced API
    retains the consumed bootstrap work registrations as the same schemas while
    adding change, admission, request, evidence, health, history, and remaining
    read operations; no alias or duplicate bridge is required.
required_outputs:
  - Tracked public MCP server registrations, exports, and strict parameter
    schemas for the native control plane.
  - Tracked request persistence, immutable replay/conflict, graph-reference
    validation, and request query behavior exposed through public MCP tools.
  - Tracked bounded job, attempt, activity, receipt, change, and work-health
    query behavior exposed through public MCP tools.
  - Tracked registry and absence assertions proving the assembled API retains
    the consumed bootstrap work operations without duplicate registrations and
    excludes generic task mutation and retired lifecycle operations.
  - Durable MCP integration tests in the focused surface-contract, request-tool,
    and native-query suites.
acceptance_scenarios:
  - 'AC1: Given the public MCP server over a temporary real engine store, invoking
    change validation/admission, job dispatch and purpose-specific completion, request,
    evidence, health, and history operations returns the native graph-aware projections
    or stable domain failures without partial mutation; verify with PROOF-011 method
    1 and the exact proof command.'
  - "AC2: Given normal lifespan startup with exclusions unset, the live MCP registry
    equals IF-010's declared catalog, including finding reads and accept/audit rejection
    operations; verify with PROOF-011 method 2 and the exact proof command."
  - 'AC3: Given the imported public server and live registry, generic task mutation
    and retired lifecycle operations are absent from registration, exports, and module
    attributes; verify with PROOF-011 method 3 and the exact proof command.'
  - "AC4: Given candidate 7805c43199f1797c61912c4ef54083802724c8ef, the exact proof
    command exits 0 with 36 passing tests, the scoped MCP source/tests remain clean
    before and after, and HEAD remains unchanged; classify the adjacent broad-suite
    failure as pre-existing, outside this node's scoped MCP source/tests, and non-blocking
    to this admitted proof."
proof_readiness:
  disposition: pass
  evidence: The exact admitted public-boundary proof runs at the candidate with
    a temporary engine store only below the boundary and needs no tracked setup
    or generated output; observed result is exit 0 with 36 passed in 15.57s.
  commands:
    - uv run pytest -n0
      serve/mcp-kanban/tests/test_mcp_surface_contract.py::TestFromAC_ToolRegistryContract
      serve/mcp-kanban/tests/test_mcp_surface_contract.py::TestFromAC_LegacyRegistrationAbsence
      serve/mcp-kanban/tests/test_mcp_surface_contract.py::TestProof011NativeControlPlane
      serve/mcp-kanban/tests/test_mcp_surface_contract.py::TestNativeAdmissionErrors
      serve/mcp-kanban/tests/test_mcp_surface_contract.py::TestFinishAcceptSchema
      serve/mcp-kanban/tests/test_mcp_request_tools.py
      serve/mcp-kanban/tests/test_mcp_native_query_tools.py -q
tracked_scope:
  clean: true
  evidence: At candidate 7805c43199f1797c61912c4ef54083802724c8ef, scoped MCP
    source/tests were clean before and after the 36-test proof run and HEAD was
    unchanged; source inspection found no implementation, test, documentation,
    generated artifact, fixture, harness, client, wiring, or
    proof-infrastructure delta for this node.
review:
  disposition: pass
  evidence: At the exact candidate, server.py registers exactly 26 native MCP
    tools, matching IF-010's declared 26-tool catalog, including finding reads
    and accept/audit rejection operations. TestFromAC_ToolRegistryContract
    asserts this set against the live post-lifespan registry; its stale function
    name does not affect the executed 26-entry set equality assertion.
    TestFromAC_LegacyRegistrationAbsence confirms generic task tools and
    finish_shape are absent from registration and module attributes.
    TestProof011NativeControlPlane exercises change, admission, job, request,
    evidence, health, and completion contracts end to end without partial
    mutation, while TestNativeAdmissionErrors and TestFinishAcceptSchema cover
    stable failures and strict acceptance schemas. DN-009 completes the bounded
    job-dispatch bridge produced under DN-004 by adding change, admission,
    request, evidence, health, history, and remaining read tools to the same
    registry without an alias or duplicate bridge. The exact proof command exits
    0 with 36 passed; scoped MCP source/tests are clean before and after; HEAD
    is unchanged. The adjacent broad-suite failure is pre-existing, outside the
    scoped MCP source/tests, and non-blocking to PROOF-011.
impact_closure:
  paths:
    - serve/mcp-kanban/src/owlbear_mcp_kanban/__init__.py
    - serve/mcp-kanban/src/owlbear_mcp_kanban/models.py
    - serve/mcp-kanban/src/owlbear_mcp_kanban/server.py
    - serve/mcp-kanban/tests/test_mcp_surface_contract.py
    - serve/mcp-kanban/tests/test_mcp_request_tools.py
    - serve/mcp-kanban/tests/test_mcp_native_query_tools.py
  authority_targets:
    - DN-009
    - REQ-020
    - REQ-009
    - REQ-015
    - REQ-016
    - REQ-017
    - NEG-001
    - KEEP-006
    - KEEP-007
    - MOD-002
    - MOD-008
    - IF-010
    - IF-001
    - IF-002
    - IF-003
    - IF-004
    - IF-015
    - DN-004
    - RISK-005
    - PROOF-011
