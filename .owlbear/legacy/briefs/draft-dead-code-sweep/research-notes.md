# Research Notes — Dead Code Sweep

## Verified Findings

### 1. `serve/orchestrator/` — Confirmed Dead, Safe to Delete

- **No external imports.** Grep of `serve/*/src/**/*.py` confirms no other package imports from `owlbear` (the orchestrator package) or `owlbear_orchestrator`.
- **Self-contained package.** Dependencies: `agent-client-protocol>=0.9.0`, `pydantic>=2.13.3`, `typer>=0.25.1`. None are shared with other packages in a way that removal would affect them.
- **Root pyproject.toml refs:**
  - Line 43: `"serve/orchestrator/src"` in ruff `src` list
  - Workspace member via glob `members = ["serve/*"]` — no explicit entry to remove; glob auto-excludes after deletion.
- **Test fixture:** `tests/fixtures/mock_acp_agent.py` — only consumer is orchestrator E2E tests.

### 2. `owlbear-project.json` — Partially Dead

- **Root file:** 3-field JSON (`name`, `owlbear_path`, `created_at`). Generated by `setup/init.py`.
- **`seed/owlbear-project.json`:** Template for consumer projects.
- **Live consumer (out of scope):** `serve/knowledge/src/owlbear_knowledge/scope_transfer.py` reads it at runtime (line 47) for KB path resolution. Has `OWLBEAR_GLOBAL_KB_PATH` env var fallback.
- **`setup/init.py` references:**
  - Line 58: in `_SKIP_IF_EXISTS_REL` frozenset
  - Lines 171–176ish: `_write_project_json()` function definition
  - Line 380: dispatch block `if rel_posix == "owlbear-project.json":`
- **Doc references:** `setup/setup-guide.md` (table row), `setup/sharing-guide.md` (table row), `README-consumer.md` (file list mention).

### 3. Copilot CLI / ACP in Live Docs & Skills

| File | Lines | Content |
|------|-------|---------|
| `README.md` | 16 | Copilot CLI prerequisite |
| `README.md` | ~35 | Directory layout row for orchestrator |
| `README.md` | 65 | Orchestrator description |
| `README.md` | 86+ | "All commands require the Copilot CLI" + CLI section |
| `.github/copilot-instructions.md` | 5 | "built around Copilot CLI" |
| `share/skills/r-architecture-standards/SKILL.md` | 13, 16–17, 157 | ACP dispatch model, orchestrator in package table |
| `share/skills/w-research/SKILL.md` | 28 | "Copilot CLI" in stack description |
| `.owlbear/doc-index.md` | auto | References README sections (auto-regenerates) |

### 4. Opportunistic Observations

- `tests/test_package_boundary.py` lines 36–37 may reference orchestrator co-packaging — needs verification during implementation.
- No other dead-code clusters found. Searches for `deprecated`, `UNUSED`, `TODO` surfaced only legitimate usage.

## Candidate Implications

- Removing orchestrator eliminates the only `agent-client-protocol` consumer — that dep can be dropped from any shared config.
- `r-architecture-standards` needs a rewritten intro paragraph — the current "Agents are `.agent.md` files dispatched by the orchestrator via ACP" is the project's actual architecture statement. Replacement should describe VS Code's native agent dispatch model.
- `.github/copilot-instructions.md` identity paragraph shapes how all agents understand the project — updating "built around Copilot CLI" is high-signal.

## Open Research Questions

1. Does `tests/test_package_boundary.py` have other orchestrator-specific content beyond lines 36–37, or is the whole file about co-packaging that no longer applies?
2. Are there any git hooks, CI workflows, or Makefile entries that reference the orchestrator CLI entry point (`uv run owlbear`)?
3. Should `r-architecture-standards` mention the orchestrator's removal context, or just silently describe the current (VS Code agent) model?
