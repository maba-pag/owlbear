## 1. Core Tombstone Lifecycle

- [ ] 1.1 Implement one `MemoryEngine` eligibility authority for non-negative whole-day cutoffs, preview counts, and best-effort physical purge of deleted entries only. Advance `Purge eligibility uses an ephemeral whole-day threshold`, `Purge permanently removes only eligible tombstones`, and `Purge returns a reconciled outcome`; no feature prerequisite. Prove exact-cutoff and zero-day eligibility, too-recent/non-deleted preservation, already-absent reconciliation, partial unlink failure, and count reconciliation with focused core tests.
- [ ] 1.2 Guard the memory engine's mtime check/reload and all shared cache/index reads, replacements, and mutations with one re-entrant instance lock without changing ordinary delete outcomes. Advance `Deleted memories remain tombstones until explicit purge` and protect the core purge invariants; prerequisite: 1.1's engine ownership shape. Prove pending hard-delete and non-pending soft-delete remain intact, then force purge interleavings with an mtime-triggered reload and an ordinary mutation to show coherent indexes and no deadlock.

## 2. Cockpit Purge Contract

- [ ] 2.1 Expose separate project-wide Memory preview and purge actions with strict non-negative integer request validation and typed count responses, delegating all classification and deletion to `MemoryEngine` and accepting no filter criteria. Advance `Purge scope is project-wide and filter-independent`, `Purge preview supports an informed irreversible decision`, and `Purge returns a reconciled outcome`; prerequisites: 1.1 and 1.2. Prove valid 30-day and zero-day requests, rejection of negative/fractional/empty/nonnumeric payloads, preview non-mutation, partial outcomes, and unchanged existing memory routes with focused Cockpit API tests.

## 3. Memory Maintenance Workflow

- [ ] 3.1 Add the frontend purge client and a Memory-owned flow that initializes each request to 30 days, validates whole numbers from zero upward, sequences preview responses by the current value, executes the exact previewed threshold, retains the result receipt, and refreshes entries. Advance `Purge eligibility uses an ephemeral whole-day threshold`, `Purge preview supports an informed irreversible decision`, and `Cockpit refreshes after purge and preserves the receipt`; prerequisite: 2.1. Prove stale preview responses cannot enable confirmation, invalid input cannot call purge, cancellation is non-mutating, and success/partial failure refreshes while preserving purged/skipped/failed feedback.
- [ ] 3.2 Present `Purge deleted (N)` as a compact secondary Memory header action derived from unfiltered entries, visible-disabled at zero, with no additional primary metric. Build the irreversible PDS `PInputNumber` confirmation showing total/eligible/too-recent counts and explicit project-wide filter-independence warning. Advance `Deleted count remains maintenance context`, `Purge scope is project-wide and filter-independent`, and `Purge preview supports an informed irreversible decision`; prerequisite: 3.1. Prove restrictive state/category/agent/search filters do not change the action count, preview payload, or execution scope, and verify the complete interaction and stable layout in the assembled Cockpit at representative desktop and mobile viewports.

## 4. Aggregate Completion

- [ ] 4.1 Add an isolated full-stack browser proof harness that serves the built Cockpit frontend through the production FastAPI application with a real `MemoryEngine` and fixture-owned Memory and Kanban stores. The harness must support deterministic tombstone ages and one controlled unlink failure without replacing `MemoryTab`, the Memory HTTP routes, or `MemoryEngine`. Prerequisites: 1.1 through 3.2. Prove the harness starts without browser auto-open, keeps fixture mutations outside the workspace stores, and can drive the rendered Memory purge workflow through the real backend.
- [ ] 4.2 At one recorded commit SHA, run the domain-scoped memory, Cockpit backend, and Cockpit frontend suites plus frontend build and lint checks, then use the assembled harness from 4.1 to exercise zero-day, positive-cutoff, restrictive-filter, cancellation, and partial-outcome workflows. Prerequisite: 4.1. Completion requires every `memory-tombstone-purge` requirement to have executable evidence, strict OpenSpec validation to pass, and no regression in pending hard-delete, ordinary soft-delete, default deleted filtering, or the primary Memory metric hierarchy.
