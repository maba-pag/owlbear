## 1. Acquisition Contract

- [ ] 1.1 Define the typed acquisition request, discriminated statuses, success provenance, diagnostics, capability report, and MCP-serializable representations. Prerequisite: none. Advances: Shared single-page acquisition capability; Structured Markdown-first success result; Non-sensitive diagnostics. Observable outcome: library callers can construct and exhaustively interpret every contract outcome without side channels. Proof: focused type/model tests for validation, required fields, status-specific null handling, serialization, and secret redaction.
- [ ] 1.2 Implement pure URL/operation validation, canonical Markdown normalization and SHA-256 hashing, and diagnostic redaction around the shared contract. Prerequisite: 1.1. Advances: User-authorized web navigation; Structured Markdown-first success result; Non-sensitive diagnostics. Observable outcome: valid HTTP(S), private-address URLs are accepted while non-web schemes, credential/script inputs, downloads, and sensitive diagnostics are handled according to contract. Proof: focused boundary tests including private/loopback addresses, prohibited inputs, deterministic hashes, and representative secret material.

## 2. Persistent Session Lifecycle

- [ ] 2.1 Extend browser lifespan ownership to provide a dedicated persistent profile, bounded pending-authentication pages, clean shutdown, and environment capability reporting. Prerequisite: 1.1. Advances: Session-assisted authentication; Capability-based authentication support. Observable outcome: authentication state survives an acquisition retry, the visible page remains available when interaction is required, stale tabs do not accumulate, and unavailable optional SSO support does not disable ordinary acquisition. Proof: lifecycle tests plus a headed persistent-profile assembled check using cookie-backed protected content.
- [ ] 2.2 Preserve the proven optional Windows Microsoft SSO-extension launch path without making it an OS-wide prerequisite or adding CDP fallback. Prerequisite: 2.1. Advances: Capability-based authentication support. Observable outcome: the service accurately reports and uses Microsoft SSO integration when available while public and manual-session acquisition remain usable without it. Proof: launcher/capability tests and managed-Windows SharePoint acquisition when that environment is available.

## 3. Validated Browser Acquisition

- [ ] 3.1 Implement staged navigation with explicit deadlines, ordered HTTP(S) redirect capture, download rejection, final URL reporting, and request-scoped page management. Prerequisites: 1.1, 1.2, 2.1. Advances: User-authorized web navigation; Acquisition remains non-interactive automation. Observable outcome: one caller-authorized page and its web redirects are observable without crawling, arbitrary actions, or caller JavaScript. Proof: deterministic real-browser fixtures for redirects, intranet/private targets, unsupported schemes, downloads, and navigation failure.
- [ ] 3.2 Implement authentication, consent, trust, access-denied, unrelated-final-page, and ambiguous-page classification using combined non-sensitive browser evidence. Prerequisite: 3.1. Advances: Session-assisted authentication; Page-state validation; Non-sensitive diagnostics. Observable outcome: login or denial content never becomes plausible Markdown success, manual authentication remains possible, and failures expose the responsible stage without secrets. Proof: deterministic real-browser fixtures for each state, false-positive content cases, manual completion, retry, and diagnostics redaction.
- [ ] 3.3 Implement selector-aware rendered-content readiness using meaningful non-empty stability observations rather than elapsed delay alone. Prerequisites: 3.1, 3.2. Advances: Rendered-content readiness; Explicit content selection; Page-state validation. Observable outcome: delayed SPA content succeeds only after stability, readiness selectors are honored, and timeout or missing/empty selector states fail distinctly. Proof: deterministic real-browser fixtures for delayed replacement, continuously changing content, readiness success/timeout, content-selector miss, empty match, and shell-only pages.

## 4. Canonical Content and Links

- [ ] 4.1 Compose rendered DOM selection with the existing Python `lxml`, trafilatura, and local Markdown fallback pipeline, and return extraction failures instead of empty success. Prerequisite: 3.3. Advances: Structured Markdown-first success result; Explicit content selection; Page-state validation. Observable outcome: successful acquisition returns clean, non-empty Markdown from the intended region while raw/sanitized HTML appears only through explicit bounded diagnostics. Proof: extraction tests for headings, links, tables, noise removal, fallback behavior, empty output, and diagnostic opt-in.
- [ ] 4.2 Normalize and deduplicate discovered HTTP(S) links from the canonical content region without navigation or other side effects, then finalize stable content hashing and timestamps. Prerequisite: 4.1. Advances: Inert normalized link discovery; Structured Markdown-first success result. Observable outcome: relative and absolute links resolve against the canonical URL in document order, unsupported/fragment duplicates are excluded, and repeat content yields the same hash. Proof: focused normalization tests plus assembled assertions that linked fixture pages receive no requests.

## 5. Agent-Callable Browser Surface

- [ ] 5.1 Add a thin browser MCP acquisition tool that delegates to the core service, preserves expected non-success statuses, and serializes every result field without adding extraction or authentication policy. Prerequisites: 1.1, 4.2. Advances: Shared single-page acquisition capability; Acquisition remains non-interactive automation. Observable outcome: an MCP client and an in-process caller receive equivalent outcomes for the same page while existing interactive tools remain separate. Proof: MCP contract tests and an assembled MCP-client-to-real-browser acquisition covering success and representative non-success results.
- [ ] 5.2 Register the browser MCP entry point in active and seed workspace configuration with startup behavior that reports browser capability failures honestly. Prerequisite: 5.1. Advances: Known-page acquisition routing; Complete DDGS retirement. Observable outcome: current and newly initialized workspaces can invoke browser acquisition through the supported MCP topology. Proof: configuration/setup regression tests and a fresh-workspace MCP discovery smoke check.

## 6. DDGS Retirement and Routing

- [ ] 6.1 Remove DDGS from Python dependency metadata and lock resolution, active and seed MCP configurations, setup initialization, and any runtime bootstrap path. Prerequisite: 5.2. Advances: Complete DDGS retirement; Open-ended DDGS search is not preserved. Observable outcome: a clean install and initialized workspace contain no direct DDGS package or server while browser acquisition remains configured. Proof: dependency lock inspection, setup/config regression tests, and repository search limited to intentional historical evidence or this change record.
- [ ] 6.2 Remove DDGS grants and active recommendations from agents and system/research guidance, replacing them with the confirmed routing among built-in known-public web access, browser acquisition, and MarkItDown while preserving separate interactive browser operations. Prerequisites: 5.1, 6.1. Advances: Known-page acquisition routing; Complete DDGS retirement; Open-ended DDGS search is not preserved; Existing browser interaction remains separate. Observable outcome: agents no longer select DDGS and receive an unambiguous supported route for each owned content type. Proof: customization/config regression tests and targeted inspection of distributed agent, instruction, prompt, skill, and setup artifacts.

## 7. Aggregate Completion Proof

- [ ] 7.1 Run the focused Python lint, type, browser-package, MCP-browser, setup/configuration, and relevant workspace regression scopes, repairing only failures caused by this change. Prerequisites: 1.1-6.2. Advances: all requirements. Observable outcome: the coordinated implementation is internally consistent and introduces no domain-scoped regression. Proof: recorded successful commands using `uv run` and the repository's established browser/MCP test scopes.
- [ ] 7.2 Prove the normal product path through a running MCP client and real Playwright browser against a delayed JavaScript public page and persistent-session authenticated page, including provenance, Markdown, inert links, redirects, hashing, and structured failures. Prerequisite: 7.1. Advances: all `browser-content-acquisition` requirements and the normal invocation promise. Observable outcome: assembled behavior matches the specs rather than merely proving delegation. Proof: deterministic local fixture suite plus headed authentication retry evidence; repeat SharePoint extraction on managed Windows when available and record managed-macOS Microsoft SSO as unclaimed until separately validated.
- [ ] 7.3 Verify distribution completeness and preserve the required continuation boundary. Prerequisites: 7.1, 7.2. Advances: all `web-content-tool-routing` requirements and the proposal boundary. Observable outcome: DDGS is absent from active product surfaces, MarkItDown and interactive browser ownership remain intact, and no crawling, approval, source-registration, refresh, or knowledge-ingestion behavior is falsely claimed or partially wired. Proof: final OpenSpec validation, targeted repository search, changed-path review, and creation/confirmation of the separately owned follow-on proposal before claiming the full authenticated knowledge-ingestion promise.
