---
mode: build
candidate_revision: 38e27ae3ed5d6215ef8a5fe25c2db82470ec6452
generation:
  job_id: 300
  receipt_id: admission-f11b5af05ac3
  predecessor_job_ids: []
  predecessor_receipt_ids: []
predecessor_contracts:
  - target_node_id: DN-012
    plan_receipt_id: plan-dn-012-f11b5af05ac3
  - target_node_id: DN-013
    plan_receipt_id: plan-dn-013-f11b5af05ac3
  - target_node_id: DN-014
    plan_receipt_id: plan-dn-014-f11b5af05ac3
packets:
  - id: DN-016-PK-001
    outcome: Execute accepted IF-017 against the live self-hosting carrier under
      the sole DN-016 build claim, atomically activate `.owlbear/native`, commit
      the complete activated destination snapshot and immutable evidence, finish
      build through the destination, and leave bound live destination and
      retained-receipt deltas for read-only PROOF-017 acceptance and IF-018.
    obligations:
      - REQ-028
      - REQ-013
      - REQ-016
      - KEEP-007
    in_scope:
      - Require current accepted DN-012, DN-013, and DN-014 dependencies,
        current PROOF-013, released legacy projections including the carrier
        root, approval, the owning DN-016 sole claim and writer, no reader or
        peer process, no unresolved request or pending mutation, recovered empty
        transactions, and an empty contained destination.
      - Run the accepted DN-012 handoff, work-root, workspace, dispatch, MCP,
        startup, scaffolding, and distribution regression suite as
        non-substitutive preflight.
      - Record absent-selector and absent-destination default resolution, then
        execute the accepted public handoff operations to classify, stage, copy,
        prove, and prepare the complete typed inventory without source mutation.
      - Stop native processes, run the sealed destination probe, verify parity
        and readiness, publish activation and selector atomically, and prove
        replay returns the same identities without another publication.
      - Verify destination lock and activity sidecars are ignored while typed
        records remain visible, then commit the complete activated pre-finish
        destination snapshot, selector, and immutable evidence.
      - Complete finish_build through the destination against that commit;
        retain destination job, attempt, and coordination completion plus the
        retained change-package build receipt as the complete live post-commit
        delta; create no later commit.
      - Preserve an acceptor-readable chain binding the commit to both live
        roots and the scoped revision.
    excluded:
      - IF-017 implementation, modification, fixture work, resolver changes,
        ignore-rule changes, completion-restriction changes, or predecessor
        proof infrastructure.
      - DN-015 work, carrier deletion, source removal, dual read, fallback, hot
        swap, activated override, compatibility behavior, or legacy translation.
      - Protected-file, authority, or predecessor-plan edits and lifecycle
        writes outside public operations.
      - Post-finish commits, second activation publication, or post-activation
        source mutation.
    modules:
      - MOD-001
      - MOD-002
      - MOD-004
      - MOD-006
      - MOD-008
      - MOD-009
    interfaces:
      consumes:
        - IF-004
        - IF-005
        - IF-017
      produces:
        - IF-018
    dependencies: []
    acceptance_scenarios:
      - id: AC-1
        scenario: Given current predecessor accept receipts, current PROOF-013,
          released legacy projections, approval, the sole DN-016 writer, an
          empty destination, no peer, request, or transaction, and passing
          non-substitutive preflight, prepare records the typed inventory,
          hashes, identities, retained authority, and source-only records
          without source mutation.
      - id: AC-2
        scenario: Given another holder, unreleased projection, pending request,
          failed preflight, unclassified record, unsafe destination, stale
          identity, collision, mismatch, or source change, prepare or activation
          returns a typed failure and publishes no selector or IF-018 evidence.
      - id: AC-3
        scenario: Given prepared state, stopped peers, and unchanged source, the
          sealed probe denies normal mutation; activation publishes stable
          evidence and selector atomically; replay preserves identities;
          selector loss, corruption, mismatch, override, or split configuration
          fails closed.
      - id: AC-4
        scenario: Given activated destination state, Git excludes destination
          lock and activity sidecars while exposing typed records; destination
          completion permits only the owning finish_build, writes the declared
          destination and retained-receipt deltas, releases the writer, and
          creates no later commit.
      - id: AC-5
        scenario: Given prepare through acceptance, SHA-256 values for
          `.vscode/mcp.json`, `.vscode/settings.json`, and `share/WIRING.md`
          equal their pre-prepare values; mismatch blocks IF-018.
      - id: AC-6
        scenario: Given the scoped commit and live completion chain, the
          acceptor reads the checkout, live destination, and retained receipts
          without handoff mutation, verifies only the declared completion
          deltas, and publishes the current DN-016 accept receipt while earlier
          DN-015 plan start and dispatch remain blocked.
    impact_closure:
      paths:
        - .owlbear/changes/replace-delivery-pipeline/
        - .owlbear/changes/replace-delivery-pipeline/plans/DN-016.yaml
        - .owlbear/kanban/
        - .owlbear/native/
        - .owlbear/native-root.yaml
        - .gitignore
        - seed/.gitignore
        - setup/handoff.py
        - setup/init.py
        - serve/kanban/src/owlbear_kanban/
        - serve/kanban/tests/test_handoff.py
        - serve/kanban/tests/test_work_root.py
        - serve/kanban/tests/test_workspace.py
        - serve/kanban/tests/test_dispatch_runtime.py
        - serve/mcp-kanban/src/owlbear_mcp_kanban/server.py
        - serve/mcp-kanban/tests/test_mcp_surface_contract.py
        - serve/cockpit/src/owlbear_cockpit/main.py
        - tests/test_bootstrap_handoff_command.py
        - tests/test_cockpit_launch.py
        - tests/test_init_scaffold.py
        - tests/test_native_distribution.py
        - .vscode/mcp.json
        - .vscode/settings.json
        - share/WIRING.md
      authority_targets:
        - DN-012
        - DN-013
        - DN-014
        - DN-016
        - REQ-028
        - REQ-013
        - REQ-016
        - KEEP-007
        - MOD-001
        - MOD-002
        - MOD-004
        - MOD-006
        - MOD-008
        - MOD-009
        - IF-004
        - IF-005
        - IF-017
        - IF-018
        - RISK-015
        - PROOF-017
    proof:
      authority: PROOF-017
      boundary: Read-only DN-016 accept job in an engine-created disposable
        checkout of the exact build commit whose builder copied the live store
        and completed finish_build through the activated destination.
      preflight_commands:
        - uv run pytest -n0 serve/kanban/tests/test_handoff.py
          serve/kanban/tests/test_work_root.py
          serve/kanban/tests/test_workspace.py
          serve/kanban/tests/test_dispatch_runtime.py
          serve/mcp-kanban/tests/test_mcp_surface_contract.py
          tests/test_bootstrap_handoff_command.py tests/test_cockpit_launch.py
          tests/test_init_scaffold.py tests/test_native_distribution.py -q
          --tb=short
      builder_execution_observations:
        - Record preflight as a prerequisite, not a replacement for
          real-workspace proof.
        - Query absent selector and destination behavior; invoke prepare, query,
          probe, activate, replay, and complete against the real roots and
          immutable job identity.
        - Commit the activated snapshot and evidence before finish_build;
          afterward retain destination completion and the retained build receipt
          without another commit.
      acceptance_observations:
        - Read the exact commit, live destination, and retained change receipts
          without handoff mutation; verify inventory, identities, receipts,
          source and protected hashes, selector, configuration, replay, build
          receipt, revision, sidecars, and declared live deltas.
        - Query DN-015 gates before acceptance and inspect them after public
          finish_accept.
      allowed_lower_replacements:
        - none; real workspace roots, process configuration, and exact build
          commit required
      evidence_outputs:
        - Non-substitutive predecessor preflight result.
        - Immutable handoff, prepared, activation, replay, configuration,
          selector, and default-resolution records.
        - Git sidecar and typed-record observation plus scoped
          activated-snapshot commit.
        - Destination completion delta, retained build receipt, and exact-commit
          accept receipt.
        - Parity, denial, restart, protected-hash, and DN-015 gate observations.
    required_outputs:
      - Passing non-substitutive predecessor preflight.
      - Committed typed activated destination pre-finish snapshot excluding
        ignored sidecars, with immutable evidence and selector.
      - Scoped commit plus live destination completion and retained build
        receipt, with no later commit.
      - Read-only PROOF-017 evidence bundle and restart evidence showing the
        destination is sole authority and the source is inert.
      - Negative evidence for stale identity, unreleased projections, source or
        destination mutation, selector or configuration split, expiry, unrelated
        completion, replay instability, and sidecar drift.
    profile:
      domain: One-time live Python lifecycle-store relocation and exact-commit
        acceptance preparation.
      risks:
        - RISK-015
      tools:
        - Python 3.14
        - uv
        - pytest
        - setup/handoff.py
        - public native lifecycle tools
        - Git read-only state inspection
        - SHA-256
      skills:
        - h-python-conventions
        - h-pytest-and-linting
    context_budget:
      expected_tracked_outputs:
        - Complete activated destination snapshot excluding ignored sidecars.
        - Retained selector.
        - Immutable evidence.
        - Scoped commit.
      expected_live_post_commit_delta:
        - Destination owning job, attempt, and coordination completion.
        - Retained change-package build receipt.
      inspected_paths: 24
      interfaces:
        - IF-004
        - IF-005
        - IF-017
        - IF-018
      change_envelope: One irreversible live handoff using accepted predecessor
        implementation; any new command, interface, migration semantic,
        fallback, protected edit, post-finish commit, or DN-015 work requires
        Specification re-entry.
review:
  disposition: pass
  evidence_id: plan-review-dn-016-job300-38e27ae3-final
  evidence: Independent source review passed completeness, admitted references,
    canonical closures, dependency order, PROOF-017 boundary, and material
    expansion.
  checks:
    plan_completeness:
      disposition: pass
      evidence: Build mode is correct — DN-016 is unimplemented
        (`.owlbear/native/` and `.owlbear/native-root.yaml` absent at the
        candidate), so verification-only is ineligible; the single packet
        carries all required fields and obligations with six scenarios covering
        the live handoff and acceptance boundary.
    admitted_references:
      disposition: pass
      evidence: Every referenced ID is inside DN-016's admitted set; migration
        effects are recorded without claiming the interface-attached migration
        ID.
    impact_closures:
      disposition: pass
      evidence: All 24 paths are canonical and cover protected bytes, startup
        resolution, sidecar ignores, retained receipts, and the complete
        predecessor preflight.
    dependency_order:
      disposition: pass
      evidence: The one-packet DAG is acyclic; DN-012, DN-013, and DN-014 are
        the exact predecessor contracts, and DN-015 remains
        terminal-prerequisite blocked.
    proof_boundary:
      disposition: pass
      evidence: PROOF-017 requires the real workspace roots, exact build commit,
        no lower replacements, commit-before-finish sequencing, and exact
        destination-side acceptance evidence.
    material_expansion:
      disposition: pass
      evidence: The packet executes only admitted IF-018 production and excludes
        IF-017 implementation, carrier retirement, fallback, dual reads,
        protected edits, and post-finish commits.
