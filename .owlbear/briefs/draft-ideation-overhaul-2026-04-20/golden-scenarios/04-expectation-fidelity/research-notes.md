# Research Notes — Expectation Fidelity Scenario

## Verified findings

- The existing two-phase workflow uses `context.md`, `decisions.md`, `research-notes.md`, `synthesis.md`, and `brief.md` as the ideation blackboard.
- Planner consumes the approved parent Brief during decomposition.

## Candidate implications

- Expectation fidelity should use the existing artifact lifecycle: living signal in `context.md`, trade-offs in `decisions.md`, binding product promise in `brief.md`.
- Downstream planning should sequence or split Brief requirements, not delete them.

## Open research questions

- Live validation is still needed to prove agent behavior follows the static contract in real sessions.