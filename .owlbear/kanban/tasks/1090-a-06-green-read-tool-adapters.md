---
id: 1090
title: 'A-06: GREEN — read tool adapters'
status: todo
priority: critical
created: 2026-04-21 10:54:20.345618+00:00
updated: 2026-04-28T05:33:32.045409+00:00
tags:
- phase:mcp
- brief:a
- scope:mcp-kanban
- tdd:green
parent: 1045
depends_on:
- 1086
blocked: false
block_reason:
claimed_by:
claimed_at:
archival_reason:
archival_refs: []
---
## Brief\nBrief A (#1045) — kanban-mcp-surface-v2/brief.md §5.1–§5.3, paper-integration.md §1.1–§1.3\nModule: `serve/mcp-kanban/src/owlbear_mcp_kanban/server.py`\n\nImplement the 3 read-only MCP tool handlers in server.py: `list_tasks`, `show_task`, `pick_tasks`. Each handler: (1) deserializes MCP call args into the input model, (2) calls the corresponding AgentView method with a 1:1 passthrough, (3) serializes the engine response envelope to MCP response, (4) catches KanbanError and maps to MCP ToolError(user_message). The adapter is mechanical — no business logic.\n\nFirst GREEN impl task on server.py — establishes the adapter pattern (error mapping helper, AgentView injection) that mutation and lifecycle tasks build on.\n\n## Acceptance Criteria\n\n- [ ] AC1: `list_tasks` tool accepts the 12 Brief A §5.1 params matching `ListTasksParams` fields from `owlbear_mcp_kanban.models` (status, priority, tag, archival_reason, ids, unclaimed, blocked, parent, search, sort, reverse, limit) — no legacy `archived: bool` on MCP surface\n- [ ] AC2: `show_task` tool accepts `id: int` + `section: str` matching `ShowTaskParams` fields on the MCP surface; adapter translates to `view.show_task(task_id=params.id, section=params.section)` since the engine `AgentView.show_task` uses `task_id` — `section=\"\"` delegated to engine without adapter-side normalization\n- [ ] AC3: `pick_tasks` tool accepts `wave_size` + `max_waves` matching `PickTasksParams` fields and forwards to `AgentView.pick_tasks`\n- [ ] AC4: Input deserialization via boundary models from `owlbear_mcp_kanban.models` using `model_validate` (not `model_construct`) — boundary validation runs before engine delegation\n- [ ] AC5: `KanbanError` → `ToolError` mapping via `_map_kanban_error` helper in all three handlers\n- [ ] AC6: Handler return types are typed Pydantic response models (`ListTasksResponse`, `ShowTaskResponse`); `list_tasks` sets `output_schema` from `ListTasksResponse.model_json_schema()` — structural test following the `test_outputschema_541.py` pattern\n- [ ] AC7: No business logic in adapter — all validation delegated to engine and boundary models\n- [ ] AC8: Adjacent test suites calling `show_task` with legacy `task_id=` kwarg (`test_mcp_guidance_1089.py:226, :254, :444`) updated to `show_task(id=..., ...)` to match the Brief A surface\n\n## Architecture Review (cycle 4 — loop-breaker re-evaluation)\n\n### Prior Cycle Summary\n3 review failures (confidence .22, .30 on last two). Loop-breaker routed to backlog for architect re-evaluation. Core adapter pattern is implemented but two critical issues remained:\n1. Downstream regression: 3 tests in test_mcp_guidance_1089.py call show_task(task_id=...) — broken by Brief A interface change\n2. AC6 wire-shape proof: no executable test for MCP JSON serialization\n\n### Evaluation\n| Criterion | Assessment | Notes |\n|-----------|-----------|-------|\n| Single responsibility | PASS | Read adapter pattern for 3 tools — one domain (MCP boundary) |\n| Interface clarity | PASS (after refine) | AC2 now specifies the id→task_id kwarg translation between MCP surface and engine |\n| Dependency correctness | PASS | Parent #1045, dependency #1086 — both archived (done) |\n| Module layering | PASS | Adapter → AgentView → Engine, no upward imports |\n| TDD compliance | PASS | Test-writer processes at `todo`; existing RED suite needs kwarg fix (see below) |\n| KISS/YAGNI | PASS | Mechanical adapter, no speculative features |\n| Premise challenge | PASS | Brief A mandates these adapters; boundary models exist in `models.py` |\n| Pattern consistency | PASS | Uses `_map_kanban_error` + AgentView injection; AC6 follows `test_outputschema_541.py` pattern |\n| Security surface | PASS | Read-only tools, input validated by boundary models (`extra=\"forbid\"`) |\n| Single domain | PASS | MCP adapter domain only |\n\n### Critical Bug Found\n**server.py:336** calls `view.show_task(id=params.id, section=params.section)` but `AgentView.show_task` signature (engine.py:2191) is `show_task(self, task_id: int, section: str | None = None)`. The `id=` kwarg will raise `TypeError` at runtime. This is hidden by mock-based tests (mocks accept any kwargs). The fix is `view.show_task(task_id=params.id, section=params.section)` — the adapter must translate between the MCP surface name (`id`) and the engine name (`task_id`). AC2 now specifies this translation explicitly.\n\n### AC6 Narrowing Rationale\nThe original AC6 (\"response envelopes serialized as MCP content JSON text\") required testing FastMCP's auto-serialization framework — not our adapter code. The adapter's actual serialization responsibility is: (a) return typed Pydantic models, (b) set `output_schema` on `list_tasks`. Both are structurally verifiable. The repo has an established pattern at `serve/mcp-knowledge/tests/test_outputschema_541.py` for exactly this kind of proof. AC6 now references that pattern.\n\n### AC8 Rollout Scope\nThe 3 callers in test_mcp_guidance_1089.py are test code using the old `show_task(task_id=...)` interface. Since #1090 owns the interface change, #1090 owns the rollout. After the AC2 bug fix (id→task_id translation), these callers must be updated to `show_task(id=..., ...)` to match the MCP surface. The guidance tests use a real engine — they will serve as integration proof that the adapter correctly translates `id` to `task_id`.\n\n### Instruction Drift Note\n`r-pipeline-protocol` line 161 still references `show_task(task_id=\"{id}\")`. This is a docs concern, not a code concern — out of scope for this GREEN task. A follow-up `type:docs` task should update pipeline instructions to match the Brief A surface.\n\n### Coverage Note\n`owlbear_mcp_kanban.server` has 10+ tool handlers. This task covers 3 read handlers + error helper. 33% module coverage is expected and not a defect.\n\n### Challenge Results\n- Challenger: block (confidence 0.27)\n- Key concerns: (1) runtime kwarg mismatch id vs task_id — ACCEPTED, AC2 now specifies translation; (2) downstream callers understated, instruction drift — PARTIALLY ACCEPTED, AC8 covers test callers, instruction drift noted as follow-up; (3) AC6 still unproved — ACCEPTED, AC6 now references established output_schema test pattern; (4) coverage overclaims safety — NOTED, but mock-heavy suite is #1086's design, not in scope\n- Architect response: revised AC addresses all critical concerns. The kwarg bug is real and was the root cause of the guidance test failures.\n\n### Verdict: REFINE → APPROVE\n### Action Taken: Rewrote AC (8 lines) to address kwarg translation bug, boundary model validation, output_schema proof pattern, and guidance test rollout. Advancing to `todo`.
[[2026-04-28]]
## Architecture Review (cycle 4 — loop-breaker)\n\nRefined AC from 7 to 8 lines. Key changes:\n\n1. **AC2 bug fix**: Found critical runtime bug — `view.show_task(id=...)` raises TypeError because engine uses `task_id`. AC2 now mandates `view.show_task(task_id=params.id, ...)` translation.\n2. **AC6 narrowed**: Structural output_schema proof (following `test_outputschema_541.py` pattern) replaces vague \"wire-shape\" requirement.\n3. **AC8 added**: Rollout of `show_task(id=...)` to 3 callers in `test_mcp_guidance_1089.py`. These use a real engine → integration proof for the kwarg translation.\n4. **AC4 clarified**: `model_validate` (not `model_construct`) explicitly required.\n\nChallenger (block, 0.27) found the kwarg mismatch — accepted. All 4 concerns addressed in refined AC.