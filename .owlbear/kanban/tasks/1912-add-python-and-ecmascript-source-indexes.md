---
id: 1912
title: Add Python and ECMAScript source indexes
status: collect
priority: medium
created: 2026-07-12T03:03:56.818108+02:00
updated: 2026-07-12T03:38:22.131528+02:00
tags:
  - scope:tools
  - feature
parent: 1914
depends_on: []
ac:
  - '`uv run py-index` deterministically regenerates a Markdown inventory of Python
    modules, top-level interfaces, and imports without importing project code.'
  - '`uv run ts-index` deterministically regenerates a Markdown inventory of TS/TSX/JS/JSX
    modules, top-level interfaces/exports, and imports using syntax parsing rather
    than regex.'
  - Both committed indexes are advisory, skip generated/vendor/test/work-state 
    paths consistently, and treat source as authoritative.
blocked: false
block_reason:
claimed_at:
archival_reason:
archival_refs: []
---
## Objective
Add committed, advisory source indexes generated by separate `py-index` and `ts-index` commands. `ts-index` covers TS, TSX, JS, and JSX. Source remains authoritative.

## Scope
In: `serve/tools`, focused durable tests, generated `.owlbear/py-index.md` and `.owlbear/ts-index.md`.
Out: agent guidance and ideation integration.

Proof guidance: run focused tools tests and both commands against the workspace.

[[2026-07-12T03:31:18+02:00]]
## Builder Notes
- Added fixed-output, always-regenerate `doc-index`, `py-index`, and `ts-index` commands plus parallel `uv run indexes`.
- Python uses `ast`; TS/TSX/JS/JSX use tree-sitter. Generated indexes are advisory and source remains authoritative.
- Shared exclusions cover work state, tests, vendor/public/generated/build outputs; parse errors are disclosed.
- Changed tools source/tests/docs, `uv.lock`, and committed `.owlbear/*-index.md` artifacts.
- Proof: `uv run pytest serve/tools/tests -q -n 0` -> 75 passed; ruff check/format passed; real `uv run indexes` succeeded with exclusion scans empty.
- Builder challenger: PASS after exact exclusion fixes.

[[2026-07-12T03:38:22+02:00]]
## Verify Notes
- Exercised `doc-index`, `py-index`, `ts-index`, and parallel `indexes` through their public CLI boundary.
- Individual and aggregate output hashes matched, proving deterministic concurrent generation.
- Verified fixed output locations, source-authoritative headers, exclusions, parse-error disclosure, and TS export-only forms including default and type exports.
- `uv run pytest serve/tools/tests -q -n 0` -> 76 passed.
- Package-wide ruff check/format passed.
- Verifier challenger: PASS after export completeness repair.
- No replacement or mock used at the command boundary.
