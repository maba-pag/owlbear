# agents/

Transition placeholder directory. The authoritative agent definitions live in
`.github/agents/`. This directory will become the primary location during the
v2 migration. VS Code settings point to both locations.
