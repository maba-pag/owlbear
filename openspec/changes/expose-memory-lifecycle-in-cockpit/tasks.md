## 1. Domain Lifecycle Semantics

- [ ] 1.1 Implement and test state-preserving edits for contested, disputed, and stale entries while retaining deleted rejection and approved-to-curated behavior, and recompute score when confidence changes. Advances **Detail-aligned editing** and **Score-led memory overview**; prerequisite: none; outcome: every live state is editable without an exceptional entry escaping review or leaving score-led ordering stale; proof: focused memory engine tests cover each source state, preserved lifecycle metadata, and confidence-derived score.
- [ ] 1.2 Correct and test exceptional-state transitions so dispute and resolution clear `contested_by_task`, stale resolution resets only `didnt_use_count`, and all other assessment history remains unchanged. Advances **Accurate contested provenance**, **Fresh stale recovery window**, and **Human-only exceptional-state resolution**; prerequisite: 1.1; outcome: resolved entries carry coherent provenance and stale entries receive a fresh non-use window; proof: state-machine and voting/scoring tests assert persisted values across fresh engine reads.

## 2. Adapter Authority And API Contract

- [ ] 2.1 Preserve agent restrictions in the MCP curation adapter after the engine becomes permissive. Advances **Human-only exceptional-state resolution**; prerequisite: 1.1; outcome: `curate_memory` still rejects contested, disputed, and stale entries and no MCP resolve tool exists; proof: MCP tool tests exercise the real adapter for all three states and surface-contract tests verify no resolve registration.
- [ ] 2.2 Expand the Cockpit memory response with `outstanding_count`, `score`, and `contested_by_task` while keeping `unremarkable_count` and `didnt_use_count` absent. Advances **Operator-relevant memory details**, **Complete lifecycle-state visibility**, and **Score-led memory overview**; prerequisite: none; outcome: Cockpit receives every field needed by the confirmed UI without internal negative counters; proof: route contract tests assert exact response fields and all seven state values.
- [ ] 2.3 Add and test the OCC-protected Cockpit resolve endpoint, delegating all transition behavior to `MemoryEngine.resolve()`, and verify Cockpit editing reaches exceptional states while MCP cannot. Advances **Human-only exceptional-state resolution** and **Detail-aligned editing**; prerequisites: 1.2, 2.1, and 2.2; outcome: the human API can edit and resolve exceptional entries with standard conflict handling; proof: route tests cover success for each exceptional state, invalid source states, stale concurrency tokens, and returned lifecycle metadata.

## 3. Cockpit Operator Workflow

- [ ] 3.1 Update frontend memory types, state filters, overview formatting, and sorting for all seven states and score-first ordering with existing tie-breakers. Advances **Score-led memory overview** and **Complete lifecycle-state visibility**; prerequisite: 2.2; outcome: the overview shows title, scope agents, categories, and two-decimal score without confidence markers or score colors; proof: focused component tests cover field rendering, score ordering, tie-breaks, and exceptional-state filtering.
- [ ] 3.2 Expand details and align edit mode so all confirmed metadata stays visible, only title/content/categories/confidence/scope agents are editable, Outstanding marks use a star icon and count, and deleted entries remain read-only. Advances **Operator-relevant memory details** and **Detail-aligned editing**; prerequisites: 2.2 and 3.1; outcome: expanded and editing views present a consistent information structure without exposing internal counters; proof: component tests assert overview/detail duplication, editable controls, read-only fields, exceptional-state preservation, and deleted behavior.
- [ ] 3.3 Add contested-task navigation and explicit resolve actions for contested, disputed, and stale entries using the shared task-detail event and Cockpit resolve client. Advances **Contested-task navigation** and **Human-only exceptional-state resolution**; prerequisites: 2.3 and 3.2; outcome: valid task references open task detail, null or malformed references remain safe, and successful resolution refreshes the entry to approved; proof: component and shell integration tests cover event dispatch, fallback rendering, action visibility, success, and OCC error feedback.

## 4. Completion And Documentation

- [ ] 4.1 Update memory and MCP documentation to describe state-preserving Cockpit edits, human-only resolution, corrected contested provenance, stale counter reset, and unchanged MCP restrictions. Advances aggregate capability clarity; prerequisites: 1.2, 2.1, and 2.3; outcome: documentation matches shipped lifecycle and authority boundaries; proof: documentation review against the capability spec and implemented route/tool surfaces.
- [ ] 4.2 Run domain, MCP-memory, Cockpit backend, and frontend test scopes, then verify the assembled Memory workflow in a browser with approved, contested, disputed, stale, and deleted fixtures at desktop and mobile viewports. Advances all capability requirements; prerequisites: 3.3 and 4.1; outcome: score ordering, complete details, edit layout, task navigation, exceptional resolution, and non-overlapping responsive presentation work together; proof: passing scoped commands plus screenshots and interaction evidence for representative states.