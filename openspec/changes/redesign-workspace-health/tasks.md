## 1. Kanban Health Evidence

- [ ] 1.1 **Build complete task-file health evidence.** Advances `Complete task-file validation` and the read-only portion of `Workspace health API`. Prerequisites: none. Outcome: Kanban returns normalized per-path evidence and every safely determinable persisted-field violation for active/archive tasks, including explicit encoding and I/O findings, without changing the filesystem. Proof: focused Kanban tests use real temporary task trees, assert multiple findings from one readable file, assert unreadable files are not clean, and compare before/after filesystem snapshots.

- [ ] 1.2 **Add board-wide task graph classification.** Advances `Task graph integrity`, `Module-aware status presentation`, and `Deterministic task duplicate repair` classification. Prerequisites: 1.1. Outcome: complete same-ID sets across both directories are classified atomically as repairable or unresolved, and graph analysis reports missing parent/dependency/archival targets, self-reference, and dependency cycles. Proof: table-driven Kanban tests cover same-directory, cross-directory, heterogeneous 3+ record sets, invalid references, and cycles with exact status/count/detail assertions.

- [ ] 1.3 **Add independent request-storage health.** Advances `Decision and action request integrity` and focused `Workspace health API` reads. Prerequisites: task identity lookup from 1.2. Outcome: every structured pending/resolved request is checked without log-only disappearance, duplicate request IDs and missing active/archive owners are reported, and location/resolution drift is classified. Proof: focused Kanban tests cover malformed records, duplicate IDs, archived owners, missing owners, and both location mismatch directions.

## 2. Deterministic Kanban Repair And Maintenance

- [ ] 2.1 **Implement convergent task repair and archive reconciliation.** Advances `Deterministic task duplicate repair`, `Synchronous convergent repair contract`, and task portions of `Health and maintenance separation`. Prerequisites: 1.1 and 1.2. Outcome: repair rediscovers complete sets, revalidates every candidate before mutation, applies the exact delete/quarantine/move matrix, never renumbers IDs, records terminal per-item outcomes, and produces a post-repair task-health snapshot. Proof: real-filesystem Kanban tests cover every matrix row, unique-largest and tied-largest multi-copy sets, changed-before-operation candidates, no-overwrite failures, repeated repair, no action-request creation for duplicate quarantine, and post-scan timing/order.

- [ ] 2.2 **Decompose generic Kanban cleanup into explicit maintenance.** Advances `Health and maintenance separation`. Prerequisites: 2.1. Outcome: archive reconciliation and duplicate correction belong only to task repair, expired-claim sweeping remains an explicit Kanban operation, stale-session reconciliation remains claim maintenance, and activity compaction remains independent. Proof: focused engine tests show repair leaves claims/activity unchanged, sweep leaves storage-health conflicts unchanged, and no cleanup behavior is lost behind an ambiguous aggregate method.

## 3. Memory And Ideas Health

- [ ] 3.1 **Expose memory integrity without changing lifecycle behavior.** Advances `Memory integrity` and focused `Workspace health API` reads. Prerequisites: none. Outcome: the memory package returns unreadable paths and complete duplicate-UUID path sets while preserving current canonical loading for normal memory use; valid pending, stale, contested, disputed, and deleted states remain healthy. Proof: focused memory-engine tests cover malformed records, 2+ duplicate UUID files, all valid lifecycle states, and read-only filesystem snapshots.

- [ ] 3.2 **Add narrow ideas-document health.** Advances `Ideas integrity`. Prerequisites: none. Outcome: Cockpit can distinguish missing/empty healthy ideas from encoding and filesystem failures without modifying the document. Proof: focused Cockpit backend tests cover absent, empty, valid UTF-8, invalid UTF-8, and injected I/O failure cases.

## 4. Cockpit Health Contracts

- [ ] 4.1 **Implement root-level health aggregation and focused reads.** Advances `Workspace health API`, `Evidence-based status states`, and module result portions of `Module-aware status presentation`. Prerequisites: 1.1, 1.2, 1.3, 3.1, and 3.2. Outcome: `/health/live` is cheap, `/health` aggregates four independently isolated checks, focused module GETs return the same typed contract, successfully assembled unhealthy documents return HTTP 200, and one checker failure does not erase siblings. Proof: Cockpit route tests use real domain engines where practical and one injected failing checker, assert no storage mutation, verify root mounting before SPA fallback, and distinguish liveness from workspace scans.

- [ ] 4.2 **Expose synchronous task repair with a complete receipt.** Advances `Synchronous convergent repair contract`, `Repair feedback remains visible`, and `Health and maintenance separation`. Prerequisites: 2.1 and 4.1. Outcome: `POST /health/tasks/repair` waits for terminal repair plus post-scan, returns timing/count/outcome/unresolved/health fields, and reports orchestration failure without claiming refreshed health. Proof: Cockpit integration tests call the real Kanban repair over temporary storage and assert response ordering, partial failures, repeat convergence, and absence of any follow-up health dependency.

- [ ] 4.3 **Remove obsolete task health and generic cleanup routes.** Advances the breaking API migration in `Workspace health API` and `Health and maintenance separation`. Prerequisites: 2.2, 4.1, and 4.2. Outcome: old task scan/repair/cleanup endpoints and shallow view forwarding are deleted, explicit claim sweep/activity maintenance remain reachable outside health, and stale tests are replaced rather than compatibility aliases added. Proof: route inventory and Cockpit backend tests confirm only the agreed health family and explicit maintenance operations remain.

## 5. Cockpit Health Experience

- [ ] 5.1 **Replace task-scan polling with ordered aggregate health state.** Advances `Evidence-based status states` and `Health refresh ordering`. Prerequisites: 4.1. Outcome: initial and focused checks use gray checking/unknown states, periodic and mutation-triggered refreshes update modules, transport failure is explicit, and request generation/`checked_at` ordering prevents stale responses from overwriting newer repair evidence. Proof: hook/provider tests cover delayed initial load, partial module failure, connection loss, periodic refresh, mutation invalidation, and an old in-flight response completing after repair.

- [ ] 5.2 **Render module-aware Workspace Status.** Advances `Module-aware status presentation` and the normal workflow. Prerequisites: 5.1. Outcome: overall and task/request/memory/ideas rows have labeled lights, summary counts, correct severity precedence, unresolved details only, and a task repair action whenever repairable count is nonzero even if the row is red. Proof: component tests cover all terminal/transient states and mixed repairable/unresolved state; integrated-browser screenshots verify the compact popover hierarchy and visible gray loading state.

- [ ] 5.3 **Implement task repair flow with session-retained feedback.** Advances `Repair feedback remains visible` and frontend behavior of `Synchronous convergent repair contract`. Prerequisites: 4.2 and 5.2. Outcome: one confirmation runs all deterministic repairs, the returned task-health snapshot is merged without an immediate GET, closing overlays cannot discard the receipt, polling cannot erase it, and dismiss/replacement/reload lifetimes are explicit. Proof: frontend integration tests exercise completion, partial failure, orchestration error, dismiss, replacement, polling, and no-follow-up-fetch behavior; integrated-browser proof reproduces and closes the current disappearing-feedback defect.

- [ ] 5.4 **Remove the generic Cleanup experience and stale health code.** Advances `Health and maintenance separation`. Prerequisites: 5.3 and 4.3. Outcome: CleanupPanel, task-only scan hooks/API clients, and ambiguous copy are removed; claim maintenance is not presented as workspace corruption; no dead imports or obsolete tests remain. Proof: frontend typecheck/build, focused search, and behavior tests confirm the new health experience is the sole Workspace Status path.

## 6. Aggregate Completion

- [ ] 6.1 **Verify the assembled workspace-health promise.** Advances all `workspace-health` requirements. Prerequisites: 1.1-5.4. Outcome: the running Cockpit starts gray, settles into four evidence-based module states, repairs deterministic fixture duplicates synchronously, retains the receipt after dialog closure, exposes only residual conflicts, and keeps claims/memory lifecycle/empty ideas out of failure status. Proof: run all matched Kanban, memory, Cockpit backend, and Cockpit frontend test scopes; run frontend typecheck/build/lint; exercise the assembled desktop workflow in VS Code's integrated browser with screenshots; and strictly validate the OpenSpec change.
