{
  "name": "owlbear",
  "private": true,
  "scripts": {
    "dev": "npm --prefix serve/cockpit/web run dev --",
    "test": "npm --prefix serve/cockpit/web test --",
    "test:e2e": "npm --prefix serve/cockpit/web run test:e2e --",
    "test:e2e:all": "npm --prefix serve/cockpit/web run test:e2e:all --",
    "build": "npm --prefix serve/cockpit/web run build",
    "lint:css": "npm --prefix serve/cockpit/web run lint:css",
    "lint:html": "npm --prefix serve/cockpit/web run lint:html",
    "lint:json": "node scripts/lint-json.mjs"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "4.1.10"
  }
}
