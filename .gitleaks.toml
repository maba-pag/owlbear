# Gitleaks configuration for OwlBear
# https://github.com/gitleaks/gitleaks

[extend]
useDefault = true

# Suppress known test-fixture mock credentials in git history.
# test_github_api.py — dummy generic-api-key (commit 66e2b10)
# test_config.py    — dummy slack-app-token values (commit 3f3d545)
[[allowlists]]
description = "Test fixture mock credentials — not real secrets"
commits = [
  "66e2b1098d4b610ab4061121f8200ae0bc397bf7",
  "3f3d545a9e339a9f2eee2a1e4d75cf9863062a4b",
]
